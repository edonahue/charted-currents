# Scripts

Expected areas: `ingest/`, `normalize/`, `resolve/`, `qa/`, `publish/`, and rights-aware `assets/` processing. Ingestion should be idempotent where practical and record retrieval metadata/checksums.
