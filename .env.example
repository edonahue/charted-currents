# Charted Currents local credentials.
# Copy this file to .env and fill values locally.
# Never commit .env or real credentials.

WHG_API_TOKEN=
EUROPEANA_API_KEY=
SMITHSONIAN_API_KEY=
DPLA_API_KEY=

# Later only if a hosted Protomaps service is deliberately adopted:
# PROTOMAPS_API_KEY=
