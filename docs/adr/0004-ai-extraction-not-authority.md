# ADR 0004 — AI extraction is proposal generation, not authority

**Status:** Accepted.

AI may aggressively propose structured fields and entity matches from legitimately accessed sources. No AI candidate becomes a published historical assertion solely because a model produced it; publication requires source provenance and validation/reconstruction state.
